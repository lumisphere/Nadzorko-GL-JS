const map = new maplibregl.Map({
    container: 'map',
    style: 'https://demotiles.maplibre.org/style.json', // Use this style URL
    center: [16.4378, 43.5081], // Split, Croatia
    zoom: 12,
  });